# GCP Cloud Run

1. Construye las imagenes Docker de `frontend` y `backend`.
2. Publicalas en Artifact Registry.
3. Crea dos servicios Cloud Run.
4. Configura `VITE_API_URL` apuntando al backend.
5. Registra secretos para credenciales Firebase o base de datos.

