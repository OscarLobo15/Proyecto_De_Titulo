# {{ project_title }}

{{ description }}

Proyecto base generado con arquitectura parametrizable.

## Stack seleccionado

- Tipo: {{ project.project_type }}
- Perfil base: {{ project.project_profile }}
{% if has_frontend %}- Frontend: {{ project.frontend }}{% endif %}
{% if has_backend %}- Backend: {{ project.backend }}{% endif %}
- Autenticacion: {{ project.auth }}
- Base de datos: {{ project.database }}
- Cloud objetivo: {{ cloud_label }}
- Contenedores: {{ containers | join(", ") }}
{% if include_services %}- Servicios adicionales: {{ service_count }}{% endif %}
{% if include_ai %}- IA: {% if include_ai_backend %}LangGraph + Gemini 2.5 Flash{% else %}Modulo Agente frontend mock{% endif %}{% endif %}

## Requisitos

- Node.js 20+
- Python 3.12+
- Docker y Docker Compose, si usaras contenedores

## Ejecucion local

```bash
cp .env.example .env
{% if target_os == "mac" %}
chmod +x dev.sh
./dev.sh start
{% else %}
.\dev.ps1 start
{% endif %}
```

URLs:

{% if has_frontend %}- Frontend: http://localhost:{{ frontend_port }}{% endif %}
{% if has_backend %}- Backend: http://localhost:{{ backend_port }}
- Swagger: http://localhost:{{ backend_port }}/docs{% endif %}

## Docker

```bash
docker compose up --build
```

## Estructura

```txt
{{ project_name }}/
{% if has_frontend %}├── frontend/
{% endif %}{% if has_backend %}├── backend/
{% endif %}
{% if include_services %}
├── services/
{% endif %}
├── docker-compose.yml
├── docs/SETUP_GUIDE.md
{% if cloud != "local" %}├── deploy.py
{% endif %}{% if cloud == "gcp" %}├── deploy/gcp/cloudbuild.yaml
├── deploy/gcp/deploy.sh
├── deploy/gcp/services.json
{% elif cloud == "aws" %}├── deploy/aws/deploy.sh
├── deploy/aws/services.json
{% elif cloud == "azure" %}├── deploy/azure/deploy.sh
├── deploy/azure/services.json
{% endif %}
{% if target_os == "mac" %}├── dev.sh
├── setup.sh
{% else %}├── dev.ps1
{% endif %}
├── package.json
└── .env.example
```

{% if include_services %}
## Servicios adicionales

Este proyecto incluye `services/template`, un microservicio base para separar integraciones, agentes o procesos especializados. Puedes levantarlo con:

```bash
{% if target_os == "mac" %}
./dev.sh services
{% else %}
.\dev.ps1 services
{% endif %}
```

Endpoint:

```bash
curl http://127.0.0.1:8002/health
```
{% endif %}

## Variables de entorno

Revisa `.env.example` y completa las credenciales necesarias antes de conectar servicios reales. El paso a paso esta en `docs/SETUP_GUIDE.md`.

{% if cloud == "gcp" %}
## Despliegue GCP

El proyecto incluye `deploy.py`, `deploy/gcp/cloudbuild.yaml`, `deploy/gcp/deploy.sh` y `deploy/gcp/services.json`. Estos archivos construyen y despliegan solo los contenedores seleccionados en Cloud Run.
{% elif cloud == "aws" %}
## Despliegue AWS

El proyecto incluye `deploy.py`, `deploy/aws/deploy.sh` y `deploy/aws/services.json`. Estos archivos publican las imagenes en ECR y dejan el proyecto listo para App Runner o ECS segun los contenedores seleccionados.
{% elif cloud == "azure" %}
## Despliegue Azure

El proyecto incluye `deploy.py`, `deploy/azure/deploy.sh` y `deploy/azure/services.json`. Estos archivos publican las imagenes en ACR y despliegan Azure Container Apps segun los contenedores seleccionados.
{% else %}
## Despliegue local

El objetivo cloud seleccionado es local, por lo que `docker compose up --build` es el camino recomendado para validar la arquitectura completa.
{% endif %}

## Proximos pasos

- Reemplazar servicios mock por integraciones reales.
- Completar credenciales de autenticacion, base de datos e IA cuando apliquen.
- Agregar pruebas de dominio.
- Ajustar pipelines de CI/CD segun el cloud objetivo.
