# Firebase Auth

Este proyecto fue generado con Firebase Auth habilitado.

Completa las variables `VITE_FIREBASE_*` en `.env` antes de usar el login real. Mientras no existan credenciales validas, Firebase rechazara el inicio de sesion.

